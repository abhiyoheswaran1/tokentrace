# 0.12.0 Final Polish Plan

## Checks First

- Add failing source tests for route feedback, Settings navigation, persisted scan result, Overview trust footer, Evidence breadcrumbs, action vocabulary, and loading next steps.

## Implementation

1. Add `NavigationProgress` and mount it in the root layout.
2. Add sticky Settings section chips and improve hash scrolling.
3. Add a reusable scan-result summary that renders either the newest scan or persisted scan history.
4. Add an Overview trust footer close to the accounting cards.
5. Add Evidence breadcrumbs, safe return links, and export action labels.
6. Audit scan and Settings follow-up links for consistent action language.
7. Tighten loading copy so slow route changes explain the current work and next step.

## Verification

- Run the new focused test file first.
- Run full unit tests, TypeScript, lint, and build.
- Restart `localhost:3000` after build.
- Capture browser screenshots for Overview, Settings Scan Controls, Evidence, and mobile Overview.
